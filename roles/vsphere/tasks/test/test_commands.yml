---
- name: Check the govc command exists
  ansible.builtin.command: govc version
  changed_when: false

- name: Check the jq command exists
  ansible.builtin.command: jq --version
  changed_when: false
