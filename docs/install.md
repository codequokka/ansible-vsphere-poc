```console
$ python3 --version
Python 3.10.4

$ pipenv --version
pipenv, version 2022.6.7

$ govc version
govc 0.23.0

$ jq --version
jq-1.6
```

```console
$ pipenv install ansible pyvmomi
$ pipenv install -d ansible-lint
$ pipenv shell
```
